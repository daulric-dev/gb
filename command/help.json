{
  "name": "gb",
  "description": "monorepo git CLI",
  "commands": [
    {
      "name": "status",
      "description": "Show git status grouped by service"
    },
    {
      "name": "affected",
      "args": "[--base=main]",
      "description": "List services with changes vs a base branch"
    },
    {
      "name": "branch",
      "args": "[service] [name]",
      "description": "Create a service-scoped branch",
      "options": [
        { "flag": "--type=feat", "description": "Optional type prefix (e.g. feat, fix)" },
        { "flag": "--custom", "description": "Create a branch with a custom name (no service/type)" }
      ]
    },
    {
      "name": "commit",
      "args": "[service]",
      "description": "Stage and commit files for a service",
      "options": [
        { "flag": "--topic \"topic\"", "description": "Commit topic (prompted if omitted)" },
        { "flag": "-m \"message\"", "description": "Optional extended commit message" },
        { "flag": "--type=feat", "description": "Commit type (feat, fix, refactor, test, docs, chore, ci, perf)" }
      ]
    },
    {
      "name": "checkout",
      "args": "[branch]",
      "description": "Checkout a branch and pull latest changes"
    },
    {
      "name": "diff",
      "args": "[service]",
      "description": "Show diff for a service or all"
    },
    {
      "name": "sync",
      "description": "Delete local branches that no longer exist on remote"
    },
    {
      "name": "rebase",
      "description": "Rebase any branch onto another to sync commits",
      "options": [
        { "flag": "--from=branch", "description": "Branch to rebase (prompted if omitted)" },
        { "flag": "--base=branch", "description": "Branch to rebase onto (prompted if omitted)" }
      ]
    },
    {
      "name": "push",
      "args": "[--force]",
      "description": "Push current branch to origin"
    },
    {
      "name": "pr",
      "description": "Create a pull request (GitHub/GitLab/Bitbucket) from the current branch",
      "options": [
        { "flag": "--from=branch", "description": "Source branch (prompted if omitted, defaults to current)" },
        { "flag": "--base=branch", "description": "Target branch (prompted if omitted)" },
        { "flag": "--title \"title\"", "description": "PR title (default: last commit subject)" },
        { "flag": "--body \"body\"", "description": "PR description" },
        { "flag": "--draft", "description": "Create as a draft PR" }
      ]
    },
    {
      "name": "service",
      "args": "[list|add|edit|remove|root]",
      "description": "Manage registered services and repo root in _mr.json"
    },
    {
      "name": "run",
      "args": "<service> <script>",
      "description": "Run a package.json script in a service"
    }
  ],
  "examples": [
    "gb status",
    "gb affected --base=dev",
    "gb branch",
    "gb branch frontend \"add auth\"",
    "gb branch frontend \"add auth\" --type=feat",
    "gb branch --custom \"best branch of them all\"",
    "gb commit frontend",
    "gb commit frontend --topic \"add auth\" --type=feat",
    "gb commit frontend --topic \"add auth\" -m \"supports OAuth and email\"",
    "gb checkout",
    "gb checkout main",
    "gb diff backend",
    "gb sync",
    "gb rebase",
    "gb rebase --from=staging --base=main",
    "gb push",
    "gb push --force",
    "gb pr",
    "gb pr --base=dev --title \"add auth\"",
    "gb pr --draft",
    "gb run backend test",
    "gb service",
    "gb service add",
    "gb service edit",
    "gb service remove",
    "gb service root",
    "gb service root set",
    "gb service root clear"
  ]
}
