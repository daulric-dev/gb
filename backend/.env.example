# Copy to `backend/.env` for local development. Never commit `.env`.
#   cp .env.example .env
#
# For a containerised deployment, use `infrastructure/env.example` instead -
# Compose sets PORT, USE_REDIS and REDIS_URL itself, so that file omits them.

# --- Supabase (required) ----------------------------------------------------
# `bunx supabase start` prints all three for a local stack.
#
# SUPABASE_SERVICE_ROLE_KEY must be the service-role **JWT** (starts `eyJ`).
# A `sb_secret_...` key is accepted by PostgREST but rejected by the edge
# function gateway, which silently takes out the dashboard, class reports,
# attendance summaries and file ingest while everything else keeps working.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PUSHABLE_KEY=

# The project's JWT secret (dashboard → Settings → API → JWT Secret; local
# stacks print it too). Signs the short-lived tokens the browser and mobile app
# use to upload straight to Storage over TUS. Must belong to the same project as
# SUPABASE_URL or Storage rejects every upload.
SUPABASE_JWT_SECRET=

# Only when Storage is reachable at a different address than SUPABASE_URL -
# e.g. the API runs in a container and clients are on the host.
# SUPABASE_PUBLIC_URL=

# --- Server -----------------------------------------------------------------
PORT=3001

# Comma-separated list. Every entry is an allowed CORS origin; the first also
# supplies the auth cookie's domain. Include the Expo dev server if you are
# running the mobile app against this backend over the web.
FRONTEND_URL=http://localhost:3000

# Absolute URL of this API, used where a link back to it has to be built.
# API_URL=http://localhost:3001

# `production` marks the auth cookie Secure, so the API must then be HTTPS.
NODE_ENV=development

# Sets the auth cookie's Domain. Leave unset locally. In production it must
# cover the API's own host, or the client stores no cookie and every request
# after login is unauthenticated - with no error pointing at it.
# AUTH_COOKIE_DOMAIN=.example.com

# --- Secrets: required in production, the app refuses to boot without them ---
# Generate each with:  openssl rand -base64 32

# Chat message encryption at rest (AES-256-GCM). Optional in development;
# without it, messages are stored as plaintext and the app says so on boot.
# CHAT_ENCRYPTION_KEY=

# Rotation: keep old keys here so existing messages stay readable, and point
# CHAT_ENCRYPTION_KEY_VERSION at the one to encrypt new messages with.
# CHAT_ENCRYPTION_KEYS=v1:base64key,v2:base64key
# CHAT_ENCRYPTION_KEY_VERSION=v2

# Keyed hash for the codes students redeem to link an account to their student
# record. Changing it invalidates every unredeemed code.
# STUDENT_CLAIM_CODE_PEPPER=

# --- Optional ---------------------------------------------------------------
# Cache and chat pub/sub. Without Redis both run in-process, which is fine for
# one instance and wrong for several - replicas would not see each other's
# invalidations or messages.
# USE_REDIS=true
# REDIS_URL=redis://localhost:6379

# Multi-participant channels, gated off until the channel UI ships. DMs are
# always on and need no flag.
# CHAT_CHANNELS_ENABLED=false

# Single-school instance: skips school selection and pins every request to the
# one school. See docs: Dedicated Deployment.
# DEDICATED_DEPLOYMENT=true
