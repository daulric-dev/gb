export interface ClassItem {
  id: string;
  name: string;
  academicYearId: string;
  isClassTeacher: boolean;
  createdAt: string;
}

export interface AcademicYear {
  id: string;
  name: string;
}
