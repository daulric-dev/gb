{
    "name": "gb",
    "devDependencies": {
        "@types/bun": "^1.3.13",
        "turbo": "^2.9.6"
    },
    "private": true,
    "packageManager": "bun@1.3.13",
    "bin": {
        "gb": "./command/index.ts"
    },
    "scripts": {
        "gb": "bun ./command/index.ts",
        "dev": "turbo run dev start:dev",
        "dev:frontend": "turbo run dev --filter=frontend",
        "dev:backend": "turbo run start:dev --filter=backend",
        "build": "turbo run build",
        "build:frontend": "turbo run build --filter=frontend",
        "build:backend": "turbo run build --filter=backend",
        "lint": "turbo run lint",
        "test": "turbo run test",
        "test:frontend": "turbo run test --filter=frontend",
        "test:backend": "turbo run test --filter=backend",
        "preview": "turbo run build && cd frontend && bun --bun run start & cd backend && bun --bun run start:prod",
        "install:all": "bun --bun install",
        "db:types": "cd backend && chmod +x ./scripts/gen_types.sh && ./scripts/gen_types.sh",
        "dev:docs": "turbo run dev --filter=docs",
        "build:docs": "turbo run build --filter=docs"
    },
    "workspaces": ["backend", "frontend", "docs"],
    "overrides": {
        "webpackbar": "^7.0.0"
    },
    "dependencies": {
        "typescript": "^6.0.3"
    }
}